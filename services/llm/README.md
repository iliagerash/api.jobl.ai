# Jobl LLM Services (`services/llm`)

This directory is reserved for LLM workers, orchestration flows, and model-serving integrations.

Keep it independent from `services/api` to avoid dependency/runtime coupling between API and model workloads.
